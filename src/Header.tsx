import React from 'react';

type HeaderPropsType = {}

export const Header = (props: HeaderPropsType) => {
    return (
        <div style={{width: "100%", border: "1 px solid black", backgroundColor: "red"}}>
            header
        </div>
    );
};
